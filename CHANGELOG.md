# Changelog

本文件格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [1.2.1] - 2026-08-10

### 修复

- **修复**："所有单位/全部单位/全体单位" 全局指令被静默丢弃——现按全局更新解析（转向/定速/急舵均生效），并与具体单位指令可共存（全局为底、具体单位覆盖同名键）；无单位名的"提速到X节/加速到X节"同样作用于所有单位（无数值"提速"仍走尺寸等级能力加速）
- **修复**：同一单位同回合"放飞+降落"并存时起飞优先、降落忽略（降落预校验时该机尚未创建）
- **P0-1**："加速到X节/提速到X节/提速至X节"不再误触发全局加速——按绝对速度指令解析（转向损失照常结算），其余单位速度保持不变
- **P0-2**：链式推进输出文件名不再时间戳嵌套累积（自动剥离输入文件名末尾的时间戳段），`Scenario['ScenarioName']` 恒为原始场景名、不再被改写成文件名
- **P0-3**：`move_units()` 中继回合追加的 `Turns` 记录与 `CurrentTurnInterval` 使用实际分钟数（与 `simplot_cmd.advance_scenario` 行为一致）
- **P1-1**：起飞单位创建移到移动结算之后——坐标取母舰**推进后**位置（修复起飞位置与 `PositionTimeCreated` 时空不一致），起飞当回合仍不移动
- **P1-2**：急舵改为按单位指令段判定——仅段内含"急舵"的单位按急舵结算，不再全局连带
- **P1-3**：降落目标存在性校验——"降落至X"的目标必须存在于 Units 单位或 aircraft 清单（按 Name/IdNum 匹配），不存在时 `raise ValueError` 并给出明确中文错误信息
- **P1-4**：未起飞飞机缺 `MaxSpeed` 时默认 **100 节**（起飞速度不再为 0；命令表最大速度列同样默认填 100）
- **P2-3**：同名输出自动追加 -2/-3 序号，不再覆盖已有存档
- **P2-4**：`process()` 支持 `.SpScn` 输入（按扩展名自动选择明文/混淆解码）

## [1.2.0] - 2026-08-09

### 新增

- **飞机生命周期规则**：剧本初设时**未起飞飞机不建立单位、不写入存档**——飞机信息（名称/所属母舰/最大速度等）由 AI 在命令表中维护（`aircraft` 参数传给 `write_cmd_sheet`/`process`）；**起飞**时（命令表计划状态填"起飞"或自然语言"放飞<飞机名>"）在推进后的新存档中新建飞机单位（低空 200 米、全功率 25% 航速、航向同母舰，HarpoonV §7.2；起飞当回合不移动）；**降落**时推进后直接从存档删除该飞机单位（Units+Objects 同步删，不回写清单）；剧本推演命令表始终保留全部飞机信息（含未起飞，降落后 AI 在下一张命令表重新列出）
- 新增 `scn_tool` 辅助函数：`mk_air_unit` / `add_unit` / `remove_unit` / `find_unit`（Units/Objects 同步增删、TrackNumber/LastId 自动维护）
- 剧本推演命令表飞机表新增"所属母舰/基地"列：未起飞行当前状态=未起飞、当前速度/航向/高度=—、最大速度预填；飞行中行当前列=实际值
- 自然语言指令新增"起飞/放飞<飞机名>"（新建单位，需传 `aircraft` 清单）与"降落"（直接从存档删除单位）；高度解析扩展支持"起飞到X米"

## [1.1.0] - 2026-08-08

### 修复

- 按 HarpoonV 规则修正 D/E 级转向损失（D 级 200码/1节、E 级 100码/2节）
- 新增 F/G 尺寸等级，补充天气、损伤、飞机相关规则
- 同步更新移动计算指南、`scn_tool.py` 及剧本推演命令表示例

## [1.0.0] - 2026-08-06

### 新增

- 首个可用版本：SimPlot 鱼叉·海上指挥推演存档工具 skill
  - 直接读写 SimPlot2 存档：`.json` 明文 + `.SpScn` 混淆编码
  - 内置 Harpoon V（鱼叉）/ Command at Sea（海上指挥）运动规则
  - 支持水面舰艇、潜艇（水上/静航/非静航）、飞机
  - 自然语言指令推进回合、生成剧本初设表
  - 脚本：`scn_tool.py`（核心库）、`simplot_cmd.py`（指令系统）
  - 参考资料与示例剧本：极地行动、冰海巨兽、拉普拉塔河口海战
