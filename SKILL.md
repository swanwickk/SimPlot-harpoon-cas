---
name: SimPlot鱼叉海上指挥推演
version: "1.1.0"
description: "生成/编辑 SimPlot2 兵棋推演存档（鱼叉 Harpoon / 海上指挥 Command at Sea：剧本初设、推进回合、读改写 .SpScn/.json、计算单位移动含潜艇/飞机），自动生成舰船信息表"
---

# SimPlot 鱼叉·海上指挥推演存档工具

生成、读取、编辑 SimPlot2 桌面兵棋（Harpoon/鱼叉、Command at Sea/海上指挥）的推演存档文件，并按对应运动规则计算单位移动。**本 skill 不依赖 SimPlot2 软件即可工作**（直接操作存档文件），软件仅用于最终加载验证。

## 首次设置

1. **获取 SimPlot2 存档目录路径**（用户提供，例如 `C:\Users\XX\...\SimPlot2-3 beta 19\Scenarios`）。所有操作以该目录为 `base_dir` 参数，**不要硬编码路径**。
2. 需要 Python 3（脚本零第三方依赖，标准库即可）。
3. 确认用户使用的是**明文 `.json`（Referee 视角）**还是混淆 `.SpScn`（Blue/Red 视角）——默认读写 `.json`，`.SpScn` 由软件保存时自动生成。

## 脚本入口（scripts/）

| 脚本 | 用途 |
| --- | --- |
| `scn_tool.py` | 核心库：编解码、坐标换算、单位构造 `mk_unit`、状态识别 `detect_state`、直行移动 `move_units`、初设表生成 `write_initial_sheet`、回合输出 `write_turn_result` |
| `simplot_cmd.py` | 自然语言指令系统：`process(base_dir, src_file, text)` 解析指令→推进→输出新存档 |

用法：`sys.path.insert(0, skill的scripts目录)` 后 `import scn_tool as st; import simplot_cmd as sc`。完整规则细节见 `references/` 两份指南。

## 工作流

### A. 生成新剧本（剧本说明 → 存档 + 初设表）

1. 从剧本说明提取：时间（军事时间"1300"=13:00）、双方单位（名称/类型/航向/航速/相对位置）、阵营（惯例：法/英/盟军=Blue，德=Red）。
2. 类型统一规则：战列舰/战列巡洋舰/袖珍战列舰→`battleship`(BB)，轻巡/重巡→`cruiser`(CL/CA)，驱逐→`destroyer`(DD)。
3. 坐标：无特别说明时以德军主力舰 (0,0) 为基准，用 `st.mk_unit()` + `st.offset_yards()`（罗盘方位+码）布置相对位置。
4. 存档结构：`Scenario`（Phase=0, TypeOfMap=0 无地图/1 自定义+MapFileName）、`Time`（TurnTime==PositionTime=剧本时间）、`Turns`=[初始回合]、`Objects`=[IdNum列表]、`Units`、`Formations`={}。
5. 写入 `<剧本名>.json`（`st.write_json`），然后调用 `st.write_initial_sheet()` 生成初设舰船信息表。

### B. 按舰船信息表（计划表）推演回合

1. 读取玩家填写的舰船信息表 md：尺寸、最大速度、计划航速、计划航向（若有潜艇/飞机表，还读计划状态/计划深度/计划高度）。
2. 用表内数据配置 `sc.UNIT_MAX_SPEED`（A级快慢判定 + 75%阈值）与 `sc.CLASS_SIZE`。
3. **计划航速校验**（三种写法）：
   - "加速/减速"无数字 → 按尺寸能力最大值（转向≥45°时加速减半）
   - "加速X节/减速X节" → 校验是否在能力值内（超则按能力值）
   - 具体航速（数字）→ 校验可达性：先算转向减半+每45°损失，能达按指定值、不能达按实际可达值
4. **计划航向解析**：右转/左转X°以命令为准；直接指定航向→**最小角度转向**（Δ>180°取反向）；空白=直行；180°必有顺逆/左右注明。
5. 移动计算（核心，细节见《鱼叉规则单位移动计算指南.md》）：
   - 新航速 = 原速 + 加速(≥45°减半) − 每45°损失×⌈|Δ|/45⌉（损失按尺寸表；**水下潜艇固定 300码/1节、急舵 200码/2节**）
   - 转向≤10°免前冲；>10° 前冲+分段（单次45°）；**渐进式**：0距离不转向（航向保持）、距离<前冲不转向、距离够部分前冲→部分转向（`Course` 写实际完成航向）
   - **潜艇**：深度>0=水下（默认静航，加减速 50%）；深度=0=水上（同水面舰艇）；深度随计划状态/计划深度更新
6. 轨迹：移动前位置写入 `PastWaypointArray1`（格式 `["",X,Y,0,0,高度/深度,0,0,0,1,true,"时间"]`），前冲/转向点追加。
7. 时间推进状态保持：do_before/do_after→TurnTime 保持、Phase=2；do_next→TurnTime 同步、Turns 追加、Phase=0。
8. 输出：新存档 `<存档名>-<PositionTime>.json`（**不覆盖旧档**）+ 新舰船信息表（命名加回合时间，当前列填实际值，**计划列清空**供下回合；潜艇/飞机填回各自表）。

### C. 自然语言推进回合

```python
import sys
sys.path.insert(0, r'<skill的scripts目录>')
import simplot_cmd as sc
out, data = sc.process(base_dir, '剧本名.json', '推进一个战术回合，沙恩霍斯特右转60度加速到25节')
```

支持：回合时长（X分钟/中继回合30min/战术回合3min）、单位（全名/前2字简称/别名/IdNum/所有单位）、航向（航向XXX/右转/左转/转向）、航速（X节/加速X节/减速X节/提速）、高度（飞机米）、深度（潜艇米）、急舵（命令含"急舵"）。

### D. 直接编辑存档

读 → 改 → 写：`st.read_json()` / `st.read_scn()` → 修改 `Units`/`Time`/`Scenario` 等 → `st.write_json()` / `st.write_scn()`。**若修改 .SpScn，需同步三个文件**（见注意事项）。

## 输出格式

| 输出 | 命名 | 说明 |
| --- | --- | --- |
| 剧本初设存档 | `<剧本名>.json` | Referee 明文 JSON |
| 初设舰船信息表 | `舰船信息表-<剧本名>初设.md` | 尺寸/最大速度留空待玩家填；含潜艇/飞机时附加对应表 |
| 推进后存档 | `<存档名>-<PositionTime>.json` | 如 `冰海巨兽-1939-11-25-13-03-00.json`，不覆盖旧档 |
| 推演后信息表 | `舰船信息表-<剧本名>-<回合时间>.md` | 当前列=实际值，计划列清空 |

## 注意事项

1. **坐标单位**：文件 X/Y = 海里×100000（不是米！）；航速/航向/高度/深度 ×1000 定点存储；罗盘 0=北、X 东+、Y 北+；`dx=dist×sin(θ), dy=dist×cos(θ)`。
2. **三文件同步**：软件保存生成 `<名>.json`(Referee) + `Blue.SpScn` + `Red.SpScn`（内容同、仅 File 字段不同）。**只改 json** 即可，Blue/Red 由用户软件保存时自动生成；若直接改 .SpScn 需三文件同步。
3. **状态识别**：`TurnTime==PositionTime` 且无轨迹=do_before(初始,Phase0)；`TurnTime!=PositionTime`=do_after(Do后,Phase2)；相等且有轨迹=do_next(Next后,Phase0)。修改须保持原状态类型。
4. **Do/Next 机制**：Do=移动（时间不变，可 Undo）；Next=确认推进时间。完整回合=Do+Next。
5. 前冲距离单位是**码**（÷2025.37×100000 转文件单位）；尺寸等级能力表见《鱼叉规则单位移动计算指南.md》§2.2。
6. **尺寸↔舰种对应不固定**（由剧本/玩家表确定）；"A"默认=快速A（最大航速≥25节），点名"慢速A"才用慢速A。
7. **潜艇规则**（§2.3/§8.4）：水上=同水面舰艇；水下=前冲固定 300码/急舵 200码、45°损失 1节/急舵 2节；深度 0=水上、>0=水下（新建默认静航）；**静航加减速 50%**。
8. **飞机规则**（§8.5）：状态改变完全按玩家指令，**无校验规则**。
9. 玩家在中间回合可能直接指定改位置/航向/航速/高度/深度——**以玩家命令为准**，不一定有命令表。
10. 失败处理：脚本报错时先检查存档路径/JSON 合法性/尾部标记；坐标异常时对照 `references/` 指南的单位字段示例。
